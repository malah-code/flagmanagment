# Quickstart Validation Guide

1. Install dependencies: `npm install react-hot-toast` (inside frontend)
2. Run the application and navigate to a project's Targeting tab.
3. Toggle a flag.
4. Expected Outcome: The toggle turns into a spinner momentarily, then a green success toast appears at the bottom of the screen saying "Flag updated successfully".
