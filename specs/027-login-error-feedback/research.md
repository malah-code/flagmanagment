# Research & Decisions

- **Decision**: Use an inline red text error message directly below the password input in `Login.tsx`.
- **Rationale**: Keeps the user's eye contextually focused where the problem is.
- **Alternatives considered**: A floating toast notification (dismissed as it requires the user to look elsewhere).
